# AI Support — reference lock

## Product reference

- **Intercom Inbox:** https://www.intercom.com/helpdesk/inbox

Intercom is used as a product communication reference: one primary conversation, clear channel context, an AI suggestion that stays subordinate to the agent, and real product media presented at a confident scale. Telegram itself is not redesigned or imitated.

## Section map

- **Opening evidence** ← the real Telegram workspace fills the width before any technical explanation.
- **Principle** ← one type-only statement establishes human control over the AI draft.
- **Master flow** ← one uninterrupted walkthrough explains the relationship between the three verified surfaces.
- **Web entry** ← sticky explanation beside large desktop evidence, followed by a true mobile crop.
- **Operator layer** ← the sanitized Telegram screen returns as the central proof, with no mock dashboard.
- **Engine** ← source, approved draft and history appear as a dense evidence strip rather than KPI cards.
- **Mobile** ← content keeps the same evidence order and every media item becomes full-width.

## Interaction lock

The signature interaction is the actual message path shown in the walkthrough: open widget → send → wait → receive → reopen/history → operator draft. No decorative interaction is added to Telegram.
